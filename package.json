{
  "name": "papertrade",
  "version": "1.0.0",
  "description": "Multi-chain paper trading simulator using real market prices. No real funds, no wallets, no transactions.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check public/app.js && node --test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
